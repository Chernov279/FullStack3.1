
  # Chef's Assistant Interface Design

  This is a code bundle for Chef's Assistant Interface Design. The original project is available at https://www.figma.com/design/lYfUQISKobA85592LIUKax/Chef-s-Assistant-Interface-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  